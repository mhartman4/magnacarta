var Cart = React.createClass({
  displayName: "Cart",

  render: function () {
    return React.createElement(
      "div",
      { className: "cart" },
      "This is where the cart goes!"
    );
  }
});